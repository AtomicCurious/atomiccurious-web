name: Keep Supabase awake

on:
  schedule:
    - cron: "0 15 */3 * *"   # cada 3 días (UTC)
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call keepalive endpoint
        run: |
          curl -fsS "${{ secrets.KEEPALIVE_URL }}"